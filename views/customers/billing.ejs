<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Billing History | MR Power System</title>
  <link rel="stylesheet" href="/css/dark.css">
</head>
<body>
  <div class="container">
    <h2>Billing History for <%= customer.FullName %></h2>
    <table style="width:100%;margin-top:1rem;">
      <thead>
        <tr>
          <th>Bill #</th><th>Date</th><th>Status</th><th>Total</th>
        </tr>
      </thead>
      <tbody>
        <% bills.forEach(bill => { %>
          <tr>
            <td><%= bill.BillNumber %></td>
            <td><%= bill.BillDate ? bill.BillDate.toISOString().slice(0,10) : '' %></td>
            <td><%= bill.Status %></td>
            <td><%= bill.TotalAmount %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <a href="/customers">Back to Customers</a>
  </div>
</body>
</html> 