<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Customer | MR Power System</title>
  <link rel="stylesheet" href="/css/dark.css">
  <link rel="stylesheet" href="/css/mrpower-theme.css">
  <style>
    .modal-overlay {
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0,0,0,0.7);
      z-index: 1000;
      display: flex;
      align-items: center;
      justify-content: center;
      animation: fadeIn 0.3s;
      overflow-y: auto;
      max-height: 100vh;
    }
    .fly-modal {
      background: #181818;
      border: 2px solid #ff2d2d;
      border-radius: 18px;
      box-shadow: 0 2px 24px rgba(255,45,45,0.18);
      padding: 2.5rem 2.5rem 2rem 2.5rem;
      min-width: 350px;
      max-width: 95vw;
      width: 400px;
      animation: flyIn 0.4s cubic-bezier(.68,-0.55,.27,1.55);
      position: relative;
      max-height: 90vh;
      overflow-y: auto;
    }
    .close-btn {
      position: absolute;
      top: 10px;
      left: 10px;
      width: 28px;
      height: 28px;
      font-size: 1.1rem;
      color: #fff;
      background: #ff2d2d;
      border: none;
      border-radius: 50%;
      cursor: pointer;
      z-index: 10;
      transition: background 0.2s, color 0.2s;
      box-shadow: 0 2px 8px rgba(0,0,0,0.12);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0;
      margin: 0;
    }
    .close-btn:hover {
      background: #c40000;
      color: #fff;
    }
  </style>
</head>
<body>
  <div class="modal-overlay">
    <div class="fly-modal">
      <button class="close-btn" onclick="closeModal()">&times;</button>
      <h2 style="text-align:center;">Edit Customer</h2>
      <form method="post" action="/customers/<%= customer.id %>/edit">
        <div class="form-group">
          <label for="FullName">Full Name</label>
          <input type="text" id="FullName" name="FullName" value="<%= customer.FullName %>" required>
        </div>
        <div class="form-group">
          <label for="Phone">Phone</label>
          <input type="text" id="Phone" name="Phone" value="<%= customer.Phone %>" required>
        </div>
        <div class="form-group">
          <label for="Email">Email</label>
          <input type="email" id="Email" name="Email" value="<%= customer.Email %>" style="width:100%;height:2.5rem;padding:0.5rem 1rem;font-size:1rem;background:#181818;color:#fff;border:1.5px solid #ff2d2d;border-radius:8px;box-sizing:border-box;">
        </div>
        <div class="form-group">
          <label for="Address">Address</label>
          <input type="text" id="Address" name="Address" value="<%= customer.Address %>">
        </div>
        <div class="form-group">
          <label for="Model">Model</label>
          <input type="text" id="Model" name="Model" value="<%= customer.Model || '' %>">
        </div>
        <div class="form-group">
          <label for="VIN">VIN</label>
          <input type="text" id="VIN" name="VIN" value="<%= customer.VIN || '' %>">
        </div>
        <div style="text-align:right;margin-top:2rem;">
          <button type="submit" class="btn-primary">Update Customer</button>
        </div>
      </form>
    </div>
  </div>
  <script>
    function closeModal() {
      window.history.back();
    }
  </script>
</body>
</html> 